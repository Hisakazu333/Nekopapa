# OpenNeko Engine 桌面客户端 UI 架构设计

## Desktop Client UI Architecture Design

> 2026-05-22 规范声明：本文保留为早期桌面客户端模块架构参考。视觉语言、桌面响应式、Dock、设置页、“我的”页、Agent IDE 和日常模式的最终实现规范，以 `文档/OpenNeko_Engine_DESIGN.md` 为最高优先级。
>
> 如果本文中的旧设计与 `OpenNeko_Engine_DESIGN.md` 冲突，必须以后者为准。尤其禁止继续按本文早期草图实现 72px 移动式侧栏、手机 TabBar、emoji 图标导航、大卡片萌系仪表盘或固定截图尺寸。

**技术栈：** Qt6 / QML + C++ (libopenneko)
**视觉风格：** 桌面原生的陪伴型 Agent 工作台，详见 `文档/OpenNeko_Engine_DESIGN.md`
**目标平台：** Windows 11 / macOS / Linux

---

## 零、当前强制布局规则

本章是当前实现的硬规则，优先级高于本文后面的早期草图。

### 0.1 桌面端不能复用移动舞台

- `designWidth: 735` / `designHeight: 944` 只能作为移动或模型舞台参考，不能作为桌面主窗口布局基准。
- `stageScale` 只能用于 Live2D 舞台和 Shell 全局 Floating Dock 的视觉尺度，不能支配“我的”、设置中心、Agent IDE、日常模式的页面内容布局。
- 桌面页必须使用窗口级 `DesktopLayoutMetrics`，不能用截图绝对坐标或 `currentPage === 某页` 的尺寸补丁堆布局。
- 横向拉伸窗口时，内容增长到上限后必须停止，额外空间变成留白。

### 0.2 DesktopLayoutMetrics

桌面设置类页面必须共享这些指标：

| Metric | 固定规则 |
| --- | --- |
| `sidebarWidth` | `clamp(windowWidth * 0.20, 260, 336)`，紧凑桌面可降到 236 |
| `mainAreaX` | `sidebarWidth + dividerWidth` |
| `mainAreaWidth` | `windowWidth - mainAreaX` |
| `contentMaxWidth` | “我的”页 1168；设置中心 680-760；Agent IDE 走三栏工作台规则 |
| `contentWidth` | `min(mainAreaWidth - contentGutter * 2, contentMaxWidth)` |
| `contentX` | `mainAreaX + (mainAreaWidth - contentWidth) / 2` |
| `dockWidth` | “我的”页 `clamp(windowWidth * 0.66, 680, 1048)`；首页 / 日常页使用自己的短 Dock |
| `dockHeight` | “我的”页 58-66；首页 / 日常页 `62 * stageScale` |
| `dockBottomMargin` | “我的”页 24-30；首页 / 日常页 `42 * stageScale` |
| `dockCenterX` | `windowWidth / 2`，Dock 属于全窗口 Shell |
| `dockClearance` | “我的”页 `dockHeight + dockBottomMargin + 32`；首页 / 日常页 `dockHeight + dockBottomMargin + 16 * stageScale` |

### 0.3 “我的”页固定骨架

- 左侧 Sidebar 固定在窗口左侧，宽度 260-336。
- 主内容从 Sidebar 右侧开始，在剩余区域居中。
- 主内容最大宽度 1168，宽屏只增加留白。
- 顶部 `当前同伴` 面板宽度等于 `contentWidth`，高度 360-390。
- 下方设置区在 `contentWidth >= 900` 时两列，小于 900 时单列。
- 两列间距 48-56，分组间距 24-32。
- 底部 Dock 由 Shell 全窗口居中悬浮，不放进 ScrollView，不跟随内容容器裁切。

### 0.4 必须返工的情况

- 桌面页继续按 735px 移动比例缩放。
- 横向拉伸窗口时，侧栏、内容、Dock 各自按不同公式漂移。
- Dock 贴底、被裁切、被滚动内容遮挡，或变成手机 TabBar。
- AppKit 原生 Dock 和 QML fallback 使用两套不同内部比例。
- 设置行、按钮、图标、文字随窗口宽度继续放大。
- 为一张截图写死绝对尺寸和坐标。

---

## 一、整体架构：双窗口模式

```
┌─────────────────────────────────────────────────────┐
│                    系统托盘常驻                        │
│              (NNA 图标 + 右键菜单)                     │
│                                                     │
│    ┌──────────────┐         ┌──────────────────┐    │
│    │   主界面窗口   │ ←IPC→  │   桌宠悬浮窗      │    │
│    │  (QML App)   │         │ (透明无边框窗口)   │    │
│    └──────────────┘         └──────────────────┘    │
│          ↕                         ↕                │
│    ┌─────────────────────────────────────────┐      │
│    │        libopenneko C++ 引擎核心           │      │
│    │   (7层ODE / 记忆系统 / AI / 事件总线)      │      │
│    └─────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────┘
```

**主界面窗口：** 完整功能客户端，所有设置和深度交互在这里完成
**桌宠悬浮窗：** 用户点击"放出桌面"后出现，猫娘在桌面自由活动
**系统托盘：** 常驻任务栏，双击打开主界面，右键快捷菜单

两个窗口共享同一个 libopenneko 引擎实例，通过 Qt 信号槽通信。

---

## 二、主界面窗口布局

### 整体结构

```
┌──────────────────────────────────────────────────────────┐
│  ◉ ◉ ◉   OpenNeko Engine              ─  □  ✕          │
├────────┬─────────────────────────────────────────────────┤
│        │                                                 │
│  侧边栏 │                 内容区                          │
│  导航   │            (根据导航切换)                        │
│        │                                                 │
│ ┌────┐ │                                                 │
│ │头像│ │                                                 │
│ │Lumia│ │                                                 │
│ └────┘ │                                                 │
│        │                                                 │
│  🏠 首页 │                                                 │
│  💬 聊天 │                                                 │
│  🐾 猫娘 │                                                 │
│  ⭐ 记忆 │                                                 │
│  📊 状态 │                                                 │
│  🎮 互动 │                                                 │
│  🔌 IoT │                                                 │
│  ⚙ 设置 │                                                 │
│        │                                                 │
│        ├─────────────────────────────────────────────────┤
│ [桌面]  │  状态栏: ♥ P:0.72  ⚡ A:0.31  🍙 饱:82%  💧 水:91% │
└────────┴─────────────────────────────────────────────────┘
```

### 侧边栏（历史草图，当前实现以第零章为准）

- 本段早期草图中的 72px 图标栏不再作为“我的”页、设置中心和日常模式的实现规格。
- 当前设置类页面使用 260-336 的桌面 Sidebar，包含头像、名称、搜索和分类导航。
- 首页或陪伴舞台如需轻量导航，也必须遵守全窗口 Shell 与 Floating Dock 规则，不能恢复手机 TabBar。

### 页面详细设计

---

### 2.1 首页 (Home)

```
┌─────────────────────────────────────────────────┐
│                                                 │
│   ┌─────────────────────────────────────────┐   │
│   │                                         │   │
│   │         猫娘 Live2D / VRM 模型           │   │
│   │         (大尺寸实时渲染区)                │   │
│   │         支持鼠标摸头/戳脸交互             │   │
│   │                                         │   │
│   └─────────────────────────────────────────┘   │
│                                                 │
│   ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│   │ 心情卡片  │ │ 生理卡片  │ │ 今日摘要  │       │
│   │ 😊 开心   │ │ 🍙 饱:82% │ │ 互动12次  │       │
│   │ P: 0.72  │ │ 💧 水:91% │ │ 亲密+0.3  │       │
│   │ A: 0.31  │ │ ⚡ 力:67% │ │ 记忆+5条  │       │
│   └──────────┘ └──────────┘ └──────────┘       │
│                                                 │
│   ┌─────────────────────────────────────────┐   │
│   │  💬 "今天过得怎么样？要不要聊聊？"        │   │
│   │                          [开始聊天 →]    │   │
│   └─────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
```

- 猫娘模型占据主视觉区域，实时反映当前 PAD 状态（表情/动作）
- 三张圆角卡片展示核心数据（心情、生理、今日摘要）
- 底部猫娘的一句话（由引擎主动性触发生成）+ 快捷聊天入口

---

### 2.2 聊天页 (Chat)

```
┌─────────────────────────────────────────────────┐
│  💬 与 Lumia 的对话                               │
├─────────────────────────────────────────────────┤
│                                                 │
│        ┌──────────────────────┐                 │
│        │ 都这个点了……          │                 │
│        │ 要不要休息一下？       │  ← 猫娘消息     │
│        └──────────────────────┘  (带小头像气泡)  │
│                                                 │
│  ┌──────────────────────┐                       │
│  │ 再写一会儿吧          │  ← 用户消息           │
│  └──────────────────────┘                       │
│                                                 │
│        ┌──────────────────────┐                 │
│        │ 好吧……那我陪你。     │                 │
│        │ 不过答应我十二点前    │                 │
│        │ 一定要睡觉哦。       │                 │
│        └──────────────────────┘                 │
│                                                 │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────┐  [🐾 发送] │
│  │  输入消息...                     │            │
│  └─────────────────────────────────┘            │
└─────────────────────────────────────────────────┘
```

- 气泡式对话，猫娘侧带小头像，配色跟随猫娘代表色
- 猫娘消息气泡可附带当前情绪小标签（如 `😊+0.3` 微小浮标）
- 支持语音输入（STT）和语音播放（TTS）按钮
- 聊天过程中侧边可展开迷你状态条，实时显示 PAD 变化

---

### 2.3 猫娘页 (Characters)

```
┌─────────────────────────────────────────────────┐
│  🐾 我的猫娘                                     │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │  Lumia   │ │  Nyx    │ │  Aria   │           │
│  │  ♥♥♥♥♡  │ │  ♥♥♡♡♡  │ │  ♥♥♥♡♡  │           │
│  │ #FF7AA2  │ │ #818CF8 │ │ #FBBF24 │           │
│  │ [使用中]  │ │ [切换]  │ │ [切换]  │           │
│  └─────────┘ └─────────┘ └─────────┘           │
│                                                 │
│  ┌─────────┐ ┌─────────┐                       │
│  │  Lyra   │ │  Kana   │                       │
│  │  ♥♥♡♡♡  │ │  ♥♥♥♡♡  │                       │
│  │ #34D399  │ │ #F87171 │                       │
│  │ [切换]   │ │ [切换]  │                       │
│  └─────────┘ └─────────┘                       │
│                                                 │
│  ── 当前猫娘详情 ──────────────────────────────  │
│                                                 │
│  ┌────────────────┬──────────────────────────┐  │
│  │   Live2D 预览   │  性格雷达图               │  │
│  │                │  INT ████████████ 100    │  │
│  │   (可旋转/缩放) │  EMP █████████░░  90    │  │
│  │                │  CHS ██░░░░░░░░░  15    │  │
│  │                │                          │  │
│  │                │  PAD 基线 / 当前漂移量     │  │
│  │                │  亲密度进度条              │  │
│  └────────────────┴──────────────────────────┘  │
│                                                 │
└─────────────────────────────────────────────────┘
```

- 顶部猫娘卡片网格，每张卡片带代表色渐变边框 + 亲密度星级
- 下方展开当前选中猫娘的详细信息
- 性格雷达图（INT/EMP/CHS 三维）
- PAD 基线与当前漂移量对比
- 亲密度进度条（逻辑斯蒂曲线可视化）

---

### 2.4 记忆星图页 (Memory Palace)

```
┌─────────────────────────────────────────────────┐
│  ⭐ 记忆星图                    [2D] [3D] [列表] │
├─────────────────────────────────────────────────┤
│                                                 │
│              ✦        ·   ✦                     │
│          ·       ✦              ·               │
│     ✦ ─────── ✦        ✦ ─── ✦                 │
│          ·    │    ✦        ·                   │
│               │  ·     ✦                        │
│     ·    ✦ ── ✦ ──── ✦         ·                │
│                    ·       ✦                    │
│          ✦              ·                       │
│                                                 │
│  (OpenGL/Vulkan 3D 渲染，鼠标拖拽旋转/缩放)      │
│  (星星亮度=情感强度，连线=时间边/情感边)            │
│  (点击星星查看记忆详情)                           │
│                                                 │
├─────────────────────────────────────────────────┤
│  选中记忆: 2026-03-10 "一起看了日落"              │
│  PAD: P=0.85 A=0.42 D=0.30  │ 情感边: 3条       │
│  [查看完整对话]  [跳转时间线]                      │
└─────────────────────────────────────────────────┘
```

- 核心是 3D 星图渲染区（OpenGL/Vulkan），可旋转、缩放、漫游
- 支持 2D 平面视图和列表视图切换
- 点击星星弹出记忆详情卡片
- 可按时间/情感强度/关键词筛选

---

### 2.5 状态页 (Status Dashboard)

```
┌─────────────────────────────────────────────────┐
│  📊 生理状态仪表盘                                │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────────────────────────────────────┐   │
│  │  24小时生理曲线图 (实时滚动)               │   │
│  │  ── 饱和度(蓝) ── 水分(青) ── 精力(绿)    │   │
│  │  ── 愉悦度(红) ── 唤醒度(橙)              │   │
│  │  ▁▂▃▅▆▇█▇▆▅▃▂▁▂▃▅▆▇█▇▆▅▃▂▁            │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ PAD 3D   │ │ 双PAD    │ │ 潜意识   │        │
│  │ 实时坐标  │ │ 融合可视化│ │ 累积量   │        │
│  │ (旋转球)  │ │ Mind/Body│ │ ████░░   │        │
│  └──────────┘ └──────────┘ └──────────┘        │
│                                                 │
│  ┌──────────────────────────────────────────┐   │
│  │  ODE 层级展开 (可折叠)                     │   │
│  │  L1 感觉过滤  ████████░░  活跃             │   │
│  │  L2 基础代谢  ██████░░░░  正常             │   │
│  │  L3 能量稳态  ███████░░░  稳态死区内        │   │
│  │  L4 情感动力  █████░░░░░  轻微波动          │   │
│  │  L5 认知记忆  ████████░░  专注             │   │
│  │  L6 主动社交  ███░░░░░░░  低活跃           │   │
│  │  L7 自由意志  ██████░░░░  正常扩散          │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
```

- 顶部 24 小时生理曲线实时滚动图
- 中间三张卡片：PAD 3D 球、双 PAD 融合可视化、潜意识累积量
- 底部 7 层 ODE 状态展开面板，每层一个进度条 + 状态标签

---

### 2.6 互动页 (Interaction)

```
┌─────────────────────────────────────────────────┐
│  🎮 互动                                        │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ 🍙 喂食  │ │ 💧 喝水  │ │ 🖐 摸头   │        │
│  │          │ │          │ │          │        │
│  └──────────┘ └──────────┘ └──────────┘        │
│                                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ 😴 睡觉  │ │ 🎮 游戏  │ │ 🎵 音乐   │        │
│  │          │ │          │ │          │        │
│  └──────────┘ └──────────┘ └──────────┘        │
│                                                 │
│  ── 最近互动记录 ─────────────────────────────   │
│  14:23  喂食(猫条)     → 饱和度 +35, P +0.2    │
│  14:10  摸头(3秒)      → P +0.15, A +0.05     │
│  13:45  她主动找你聊天  → 亲密度 +0.02          │
│                                                 │
└─────────────────────────────────────────────────┘
```

- 大圆角卡片按钮网格，每个互动带萌系图标
- 点击触发动画 + 引擎事件注入
- 下方显示最近互动记录及其对生理状态的影响

---

### 2.7 IoT 页 (Devices)

- 已连接设备列表（Arduino/ESP32/树莓派）
- 每个设备的情绪映射规则配置
- 实时预览：当前 PAD → 设备输出（灯光颜色/风扇转速等）

### 2.8 设置页 (Settings)

- 通用：语言、主题色、开机自启、托盘行为
- 引擎：ODE 参数微调（高级模式）、积分器选择、Tick 频率
- AI：LLM Provider 配置（本地/云端）、TTS/STT 开关
- 隐私：加密算法选择、LDP 隐私预算 ε、数据导出/删除
- 桌宠：桌宠窗口大小、默认位置、交互热区、动画帧率
- 关于：版本信息、开源许可、团队信息

---

## 三、桌宠悬浮窗

```
         ┌──────────┐
         │  💬 气泡  │  ← 猫娘主动说话时弹出，几秒后自动消失
         └────┬─────┘
              │
         ┌────┴─────┐
         │          │
         │  猫娘    │  ← 无边框透明窗口
         │  Live2D  │     鼠标穿透（非交互区域）
         │          │     支持拖拽移动
         │          │     右键菜单（喂食/聊天/收回/设置）
         └──────────┘
              │
    ══════════╧══════════  ← 可站在任务栏上（可选）
```

**交互方式：**
- 左键拖拽：移动猫娘位置
- 左键点击猫娘身体：触发摸头/戳脸等触觉交互（根据点击位置）
- 右键：快捷菜单（喂食、喝水、聊天、查看状态、收回主界面）
- 双击：展开迷你聊天气泡输入框
- 鼠标悬停：显示当前心情小表情

**行为系统：**
- 猫娘在桌面上有自主行为（走动、坐下、打哈欠、伸懒腰）
- 行为由行为树 + ODE 状态驱动（累了会坐下，饿了会做出求食动作）
- 闲置时有呼吸/眨眼/尾巴摆动等 idle 动画

---

## 四、系统托盘

**右键菜单：**
```
┌─────────────────────┐
│ 🐾 Lumia  😊 开心    │  ← 当前猫娘 + 心情
├─────────────────────┤
│ 打开主界面           │
│ 放出桌面 / 收回桌面   │
├─────────────────────┤
│ 快捷互动 →           │  ← 子菜单：喂食/喝水/摸头
├─────────────────────┤
│ 设置                 │
│ 退出                 │
└─────────────────────┘
```

---

## 五、视觉风格指南（二次元萌系）

### 配色体系
- **主背景：** 柔和的奶白色 `#FFF8F0` / 浅粉 `#FFF0F5`
- **卡片背景：** 纯白 `#FFFFFF`，大圆角（16-20px），柔和阴影
- **强调色：** 跟随当前猫娘代表色（Lumia=#FF7AA2, Nyx=#818CF8 等）
- **文字：** 深灰 `#2D2D2D`（主文字）、中灰 `#888888`（次要文字）
- **支持深色模式：** 深色背景 `#1A1A2E` + 猫娘代表色点缀

### 装饰元素
- 猫爪形状的按钮 hover 效果
- 页面切换时的猫耳过渡动画
- 加载状态用猫娘打盹动画替代传统 spinner
- 通知气泡带猫尾巴装饰
- 滚动条用猫爪形状替代

### 字体
- 中文：思源黑体 / 思源宋体
- 英文/数字：Nunito / Quicksand（圆润可爱的无衬线体）
- 代码/数据：JetBrains Mono

### 动效原则
- 所有过渡使用缓动曲线（ease-out），时长 200-300ms
- 卡片 hover 时轻微上浮 + 阴影加深
- 数值变化使用平滑插值动画
- 避免生硬的弹窗，优先使用滑入/淡入

---

## 六、QML 组件命名规范

遵循 NNA 开发规范，所有自定义 QML 组件以 `NNA` 前缀命名：

```
NNAWindow.qml           — 主窗口容器
NNASidebar.qml          — 侧边栏导航
NNAAvatarCanvas.qml     — Live2D/VRM 渲染面
NNAChatBubble.qml       — 聊天气泡组件
NNAStatusCard.qml       — 状态卡片组件
NNAMemoryPalace.qml     — 3D 记忆星图面板
NNAPhysioDashboard.qml  — 生理仪表盘
NNAInteractionGrid.qml  — 互动按钮网格
NNAPetOverlay.qml       — 桌宠悬浮窗
NNATrayMenu.qml         — 系统托盘菜单
NNARadarChart.qml       — 性格雷达图
NNAPADSphere.qml        — PAD 3D 球
NNASubconsciousBar.qml  — 潜意识累积条
NNASettingsPanel.qml    — 设置面板
```

---

## 七、C++ ↔ QML 桥接层

```cpp
// app_controller.h — QObject 桥接引擎→QML
class NNAAppController : public QObject {
    Q_OBJECT

    // 只读生理数据 (QML 声明式绑定)
    Q_PROPERTY(float pleasure READ pleasure NOTIFY physiologyChanged)
    Q_PROPERTY(float arousal READ arousal NOTIFY physiologyChanged)
    Q_PROPERTY(float dominance READ dominance NOTIFY physiologyChanged)
    Q_PROPERTY(float satiety READ satiety NOTIFY physiologyChanged)
    Q_PROPERTY(float hydration READ hydration NOTIFY physiologyChanged)
    Q_PROPERTY(float energy READ energy NOTIFY physiologyChanged)
    Q_PROPERTY(QString currentMood READ currentMood NOTIFY physiologyChanged)

    // 用户交互 → 引擎 (QML 调用)
    Q_INVOKABLE void feedPet(const QString& foodType);
    Q_INVOKABLE void giveWater();
    Q_INVOKABLE void touchPet(float x, float y, float duration);
    Q_INVOKABLE void sendMessage(const QString& text);
    Q_INVOKABLE void toggleDesktopPet(bool show);

signals:
    void physiologyChanged();
    void chatMessageReceived(const QString& text, float p, float a, float d);
    void petActionTriggered(const QString& action);
};
```

**数据流向：**
- C++ 引擎 Tick → 更新 Q_PROPERTY → QML 自动刷新 UI
- QML 用户操作 → Q_INVOKABLE → C++ 注入引擎事件
- 严格遵循单向数据流，QML 层不直接修改引擎状态
